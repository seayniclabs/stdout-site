---
import '../styles/global.css';
import { getUserCount } from '../lib/auth';
import { getStoredLicense } from '../lib/license';

const user = Astro.locals.user;

// Auto-redirect to setup if not configured
const userCount = getUserCount();
console.log('[index] getUserCount():', userCount);
if (userCount === 0) {
  console.log('[index] Redirecting to /setup (no users)');
  return Astro.redirect('/setup');
}

// If logged in, redirect to app
if (user) {
  return Astro.redirect('/app');
}

// Otherwise show landing page (license check removed - not needed here)
---
<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Self-hosted incident companion for homelab builders and solo DevOps. AI diagnosis, living runbooks, stack auto-discovery. $149 one-time, own it forever." />
  <title>StdOut — You've already solved this</title>
  <link rel="canonical" href={new URL('/', Astro.site || 'https://stdout.seayniclabs.com').href} />
  <meta property="og:title" content="StdOut — Self-Hosted Incident Companion" />
  <meta property="og:description" content="Your incident memory. When something breaks at 2am, StdOut pulls up how you fixed it last time. Self-host for $149, own it forever." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={new URL('/', Astro.site || 'https://stdout.seayniclabs.com').href} />
  <meta property="og:image" content={new URL('/og-image.png', Astro.site || 'https://stdout.seayniclabs.com').href} />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="StdOut — Self-Hosted Incident Companion" />
  <meta name="twitter:description" content="Your incident memory. When something breaks at 2am, StdOut pulls up how you fixed it last time. Self-host for $149, own it forever." />
  <meta name="twitter:image" content={new URL('/og-image.png', Astro.site || 'https://stdout.seayniclabs.com').href} />
  <meta name="theme-color" content="#F97316" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="sitemap" href="/sitemap-index.xml" />
  <script type="application/ld+json" set:html={JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'SoftwareApplication',
    name: 'StdOut',
    applicationCategory: 'DeveloperApplication',
    operatingSystem: 'Linux, macOS, Docker',
    description: 'Self-hosted incident companion for homelab builders and solo DevOps. AI diagnosis, living runbooks, stack auto-discovery.',
    url: (Astro.site || 'https://stdout.seayniclabs.com').toString(),
    image: new URL('/og-image.png', Astro.site || 'https://stdout.seayniclabs.com').href,
    offers: { '@type': 'Offer', price: '149', priceCurrency: 'USD' },
    publisher: { '@type': 'Organization', name: 'Seaynic Labs LLC', url: 'https://seayniclabs.com' },
  })} />
  <style>@import '../styles/fonts.css';</style>
  <script defer src="https://analytics.seaynicroute.com/data" data-website-id="6794355e-d87a-4022-9062-4d473bdc4716"></script>
</head>
<body>
  <!-- Nav -->
  <nav class="ln">
    <a href="/" class="ln-brand">
      <img src="/logo.svg" alt="StdOut" width="28" height="28" />
      <span>StdOut</span>
    </a>
    <div class="ln-links">
      <a href="#how" class="ln-link">How it works</a>
      <a href="#features" class="ln-link">Features</a>
      <a href="#pricing" class="ln-link">Pricing</a>
      <a href="/use-cases" class="ln-link">Use Cases</a>
      <a href="/docs/scanner" class="ln-link">Docs</a>
      {user ? (
        <a href="/app" class="btn btn-primary btn-sm">Dashboard</a>
      ) : (
        <>
          <a href="/app/login" class="ln-link">Log in</a>
          <a href="/app/register" class="btn btn-primary btn-sm">Self-Host $149</a>
        </>
      )}
    </div>
    <button class="ln-toggle" id="lnToggle" aria-label="Menu" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </nav>

  <div class="ln-mobile" id="lnMobile">
    <a href="#how" class="ln-mobile-link">How it works</a>
    <a href="#features" class="ln-mobile-link">Features</a>
    <a href="#pricing" class="ln-mobile-link">Pricing</a>
    <a href="/use-cases" class="ln-mobile-link">Use Cases</a>
    <a href="/docs/scanner" class="ln-mobile-link">Docs</a>
    {user ? (
      <a href="/app" class="ln-mobile-link ln-mobile-cta">Dashboard</a>
    ) : (
      <>
        <a href="/app/login" class="ln-mobile-link">Log in</a>
        <a href="/app/register" class="ln-mobile-link ln-mobile-cta">Self-Host $149</a>
      </>
    )}
  </div>

  <main id="main-content">
  <!-- Hero -->
  <section class="hero" aria-label="Hero">
    <div class="hero-badge mono">INCIDENT MEMORY FOR YOUR INFRASTRUCTURE</div>
    <h1>You've already solved this.<br /><span class="hero-dim">You just don't remember.</span></h1>
    <p class="hero-sub">StdOut is your incident companion. When something breaks at 2am, it pulls up how you fixed it last time &mdash; your stack, your past resolutions, your runbooks. Stop solving the same problem twice.</p>
    <div class="hero-cta">
      <a href="/app/register" class="btn btn-primary btn-lg btn-glow">Self-Host for $149</a>
    </div>
    <p class="hero-note mono">$149 one-time. 3-4 minute setup. Your infra, your data, forever.</p>

    <!-- Scanner HUD -->
    <div class="hud">
      <div class="hud-bar">
        <div class="hud-status">
          <span class="hud-dot"></span>
          <span class="mono">SCANNING</span>
        </div>
        <span class="hud-label mono">stdout scanner v2</span>
      </div>
      <div class="hud-body mono">
        <div class="hud-line"><span class="hud-ts">00:00.12</span> Connecting to Docker socket...</div>
        <div class="hud-line"><span class="hud-ts">00:00.34</span> Discovered <span class="hud-accent">53 containers</span> across 12 compose projects</div>
        <div class="hud-line"><span class="hud-ts">00:00.51</span> Mapped 47 exposed ports, 8 networks, 3 volumes</div>
        <div class="hud-line"><span class="hud-ts">00:01.03</span> TLS cert check: 14 certs valid, <span class="hud-warn">2 expiring &lt;30d</span></div>
        <div class="hud-line"><span class="hud-ts">00:01.28</span> Network scan: 6 SNMP devices, 2 switches, 1 AP</div>
        <div class="hud-line hud-result"><span class="hud-ts">00:01.44</span> Stack fingerprint complete &mdash; <span class="hud-accent">ready for incident matching</span></div>
      </div>
    </div>
  </section>

  <!-- How it works -->
  <section class="how" id="how" aria-label="How it works">
    <h2>How it works</h2>
    <div class="pipeline">
      <div class="pipe-step">
        <div class="pipe-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M21 21l-4.35-4.35M11 19a8 8 0 100-16 8 8 0 000 16z" stroke="#FB923C" stroke-width="2" stroke-linecap="round"/></svg>
        </div>
        <div class="pipe-num mono">01</div>
        <div class="pipe-label">Discover</div>
        <p>Run the scanner. It auto-discovers your Docker containers, network devices, TLS certs, and compose projects. One command, full stack map.</p>
      </div>
      <div class="pipe-connector"><div class="pipe-line"></div></div>
      <div class="pipe-step">
        <div class="pipe-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 8v4l3 3M21 12a9 9 0 11-18 0 9 9 0 0118 0z" stroke="#FB923C" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div class="pipe-num mono">02</div>
        <div class="pipe-label">Log</div>
        <p>Something breaks. Log the incident &mdash; paste the error, tag the severity. AI matches it against your resolution history. 30 seconds.</p>
      </div>
      <div class="pipe-connector"><div class="pipe-line"></div></div>
      <div class="pipe-step">
        <div class="pipe-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l.146.146A2 2 0 0116 19H8a2 2 0 01-1.414-.586l.146-.146z" stroke="#FB923C" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div class="pipe-num mono">03</div>
        <div class="pipe-label">Diagnose</div>
        <p>AI analyzes against your specific stack context and past fixes. Ranked root causes, commands to run, logs to check. Not generic &mdash; yours.</p>
      </div>
      <div class="pipe-connector"><div class="pipe-line"></div></div>
      <div class="pipe-step">
        <div class="pipe-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" stroke="#FB923C" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div class="pipe-num mono">04</div>
        <div class="pipe-label">Remember</div>
        <p>Record the resolution. Build your runbook. Next time it breaks, StdOut finds the fix before the AI even runs. Your knowledge compounds.</p>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section class="features" id="features" aria-label="Features">
    <h2>What you get</h2>
    <div class="feat-grid">
      <div class="feat">
        <div class="feat-head">
          <svg class="feat-icon" width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M21 21l-4.35-4.35M11 19a8 8 0 100-16 8 8 0 000 16z" stroke="#FB923C" stroke-width="2" stroke-linecap="round"/></svg>
          <h3>Stack Scanner</h3>
        </div>
        <p>Auto-discovers Docker containers, compose projects, ports, networks, TLS certs, and SNMP devices. One command maps your entire infrastructure.</p>
        <a href="/use-cases/docker-incident-management" class="feat-link">Docker incident management →</a>
      </div>
      <div class="feat">
        <div class="feat-head">
          <svg class="feat-icon" width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l.146.146A2 2 0 0116 19H8a2 2 0 01-1.414-.586l.146-.146z" stroke="#FB923C" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          <h3>AI Diagnosis</h3>
        </div>
        <p>Claude analyzes your incident against your stack context and resolution history. Not generic answers &mdash; answers that know your infrastructure.</p>
        <a href="/use-cases/solo-devops-incident-management" class="feat-link">Solo DevOps workflows →</a>
      </div>
      <div class="feat">
        <div class="feat-head">
          <svg class="feat-icon" width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" stroke="#FB923C" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          <h3>Knowledge Base</h3>
        </div>
        <p>Turn resolutions into runbook pages, postmortems, and operational docs. Full-text search across everything you've ever written.</p>
        <a href="/use-cases/homelab-runbook-builder" class="feat-link">Homelab runbooks →</a>
      </div>
      <div class="feat">
        <div class="feat-head">
          <svg class="feat-icon" width="22" height="22" viewBox="0 0 24 24" fill="none"><rect x="3" y="3" width="7" height="7" rx="1" stroke="#FB923C" stroke-width="2"/><rect x="14" y="3" width="7" height="7" rx="1" stroke="#FB923C" stroke-width="2"/><rect x="3" y="14" width="7" height="7" rx="1" stroke="#FB923C" stroke-width="2"/><rect x="14" y="14" width="7" height="7" rx="1" stroke="#FB923C" stroke-width="2"/></svg>
          <h3>HUD Monitoring</h3>
        </div>
        <p>Uptime monitoring for every service in your stack. Auto-creates incidents on downtime. A control room for your infrastructure.</p>
        <a href="/use-cases/self-hosted-monitoring-dashboard" class="feat-link">Self-hosted monitoring →</a>
      </div>
      <div class="feat">
        <div class="feat-head">
          <svg class="feat-icon" width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2h1a2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" stroke="#FB923C" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          <h3>Public Status Page</h3>
        </div>
        <p>Give your users a status page that updates automatically from your HUD. No separate service, no extra config.</p>
      </div>
      <div class="feat">
        <div class="feat-head">
          <svg class="feat-icon" width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" stroke="#FB923C" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          <h3>Weekly Digest</h3>
        </div>
        <p>A weekly email summarizing incidents, resolutions, and stack health. Stay on top of your infrastructure without watching a dashboard.</p>
        <a href="/use-cases/on-call-incident-companion" class="feat-link">On-call companion →</a>
      </div>
    </div>
  </section>

  <!-- Use Cases -->
  <section class="use-cases-section">
    <h2>Built for how you actually work</h2>
    <div class="uc-grid">
      <a href="/use-cases/stdout-for-homelab" class="uc-card">
        <span class="uc-title">Homelab Infrastructure</span>
        <span class="uc-desc">Manage 10-100+ containers with AI-assisted troubleshooting</span>
      </a>
      <a href="/use-cases/stdout-prometheus-integration" class="uc-card">
        <span class="uc-title">Prometheus Integration</span>
        <span class="uc-desc">Enrich incidents with metrics from your existing Prometheus stack</span>
      </a>
      <a href="/use-cases/self-hosted-postmortem-tool" class="uc-card">
        <span class="uc-title">Post-Mortem Tool</span>
        <span class="uc-desc">Turn every incident into documentation that compounds over time</span>
      </a>
      <a href="/use-cases/stdout-vs-pagerduty" class="uc-card">
        <span class="uc-title">StdOut vs PagerDuty</span>
        <span class="uc-desc">Self-hosted alternative without the enterprise price tag</span>
      </a>
      <a href="/use-cases/stdout-trivy-vulnerability-management" class="uc-card">
        <span class="uc-title">Trivy Vulnerability Scanning</span>
        <span class="uc-desc">Container CVE detection integrated into your incident workflow</span>
      </a>
      <a href="/use-cases/self-hosted-security-incident-response" class="uc-card">
        <span class="uc-title">Security Incident Response</span>
        <span class="uc-desc">CrowdSec + Trivy + Pi-hole data in one incident view</span>
      </a>
    </div>
    <div class="uc-more"><a href="/use-cases">View all 22 use cases →</a></div>
  </section>

  <!-- Pricing -->
  <section class="pricing" id="pricing" aria-label="Pricing">
    <h2>Own it. Run it. Keep it.</h2>
    <p class="pricing-sub">Your infrastructure, your data. Self-hosted is the default path we recommend.</p>
    <div class="price-grid price-grid-primary">
      <div class="price-card featured">
        <div class="price-badge mono">RECOMMENDED</div>
        <div class="price-tier">Self-Hosted</div>
        <div class="price-amount mono">$149<span class="price-per"> once</span></div>
        <p class="price-desc">Your infra, your data, forever. Ships as a Docker image you deploy on your own hardware.</p>
        <div class="price-items">
          <div class="pi"><span class="pi-check">&#10003;</span> Everything &mdash; no feature gates</div>
          <div class="pi"><span class="pi-check">&#10003;</span> Docker Compose deploy</div>
          <div class="pi"><span class="pi-check">&#10003;</span> Unlimited stacks &amp; incidents</div>
          <div class="pi"><span class="pi-check">&#10003;</span> Unlimited storage</div>
          <div class="pi"><span class="pi-check">&#10003;</span> Your data stays on your network</div>
          <div class="pi"><span class="pi-check">&#10003;</span> No subscription, ever</div>
          <div class="pi"><span class="pi-check">&#10003;</span> Bring your own API key</div>
        </div>
        <a href="/app/register" class="btn btn-primary btn-glow" style="width:100%;">Get Self-Hosted</a>
      </div>
    </div>
    <p class="try-free-cloud"><a href="/app/register">Try free on cloud</a> &mdash; no credit card. 1 stack, 10 incidents/month, 100 MB knowledge base.</p>

    <div class="cloud-plans">
      <p class="cloud-plans-label mono">For those who prefer managed hosting</p>
      <div class="price-grid-cloud">
        <div class="price-card price-card-cloud">
          <div class="price-tier">Solo</div>
          <div class="price-amount-cloud mono">$12<span class="price-per">/mo</span></div>
          <p class="price-desc-cloud">Full StdOut in the cloud for individual homelabbers.</p>
          <a href="https://store.seayniclabs.com/products/stdout-solo" class="btn btn-secondary btn-sm-cloud" target="_blank" rel="noopener noreferrer">Subscribe</a>
        </div>
        <div class="price-card price-card-cloud">
          <div class="price-tier">Shop</div>
          <div class="price-amount-cloud mono">$24<span class="price-per">/mo</span></div>
          <p class="price-desc-cloud">Teams: shared stacks, RBAC, more seats and storage.</p>
          <a href="https://store.seayniclabs.com/products/stdout-shop" class="btn btn-secondary btn-sm-cloud" target="_blank" rel="noopener noreferrer">Subscribe</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Trust / Builder -->
  <section class="trust">
    <div class="trust-inner">
      <div class="trust-badge mono">BUILT BY A BUILDER</div>
      <h2>Built by a solo engineer who runs 53 containers on a Mac Mini.</h2>
      <p>StdOut isn't theoretical. It's the tool I built to manage my own homelab &mdash; the same infrastructure that runs production workloads, media servers, monitoring, and a dozen side projects. Every feature exists because I needed it at 2am when something broke.</p>
      <div class="trust-links">
        <a href="https://charlieseay.com" class="btn btn-ghost btn-sm">charlieseay.com</a>
        <a href="https://charlieseay.com/blog" class="btn btn-ghost btn-sm">Read the blog</a>
      </div>
    </div>
  </section>

  <!-- Bottom CTA -->
  <section class="bottom-cta">
    <div class="cta-inner">
      <img src="/logo.svg" alt="" width="48" height="48" style="margin-bottom: 20px; opacity: 0.8;" />
      <h2>Stop solving the same incident twice.</h2>
      <p class="mono" style="color: var(--text-muted); margin-bottom: 24px;">Your future self will thank you.</p>
      <div class="cta-buttons">
        <a href="/app/register" class="btn btn-primary btn-lg btn-glow">Self-Host for $149</a>
        <a href="/app/register" class="btn btn-ghost">Or try free on cloud</a>
      </div>
    </div>
  </section>
  </main>

  <footer class="lf">
    <div class="lf-links">
      <a href="/privacy">Privacy</a>
      <a href="/terms">Terms</a>
      <a href="/docs/scanner">Docs</a>
      <a href="mailto:hello@seayniclabs.com">Contact</a>
    </div>
    <span class="mono" style="font-size: 11px; color: var(--text-muted);">StdOut by <a href="https://seayniclabs.com" style="color: var(--text-secondary);">Seaynic Labs</a></span>
  </footer>
</body>
</html>

<style>
  /* ── Nav ────────────────────────────────────── */
  .ln { display: flex; justify-content: space-between; align-items: center; padding: 14px 24px; max-width: 1100px; margin: 0 auto; }
  .ln-brand { display: flex; align-items: center; gap: 10px; font-weight: 700; font-size: 17px; color: var(--text); text-decoration: none; }
  .ln-links { display: flex; align-items: center; gap: 20px; }
  .ln-link { font-size: 13px; color: var(--text-muted); text-decoration: none; font-family: var(--font-mono); }
  .ln-link:hover { color: var(--text); }
  .btn-sm { padding: 7px 16px; font-size: 12px; }
  .btn-lg { padding: 14px 32px; font-size: 15px; }

  /* ── Glow button ───────────────────────────── */
  .btn-glow {
    box-shadow: 0 0 20px var(--accent-glow), var(--shadow-sm);
  }
  .btn-glow:hover {
    box-shadow: 0 0 40px var(--accent-glow), 0 0 80px rgba(249, 115, 22, 0.1), var(--shadow-md);
  }

  /* ── Ghost button ──────────────────────────── */
  .btn-ghost {
    background: transparent;
    color: var(--text-secondary);
    border: 1px solid var(--border);
    padding: 10px 20px;
    border-radius: var(--radius-sm);
    font-size: 13px;
    font-weight: 600;
    font-family: var(--font-ui);
    cursor: pointer;
    transition: all 0.15s ease;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
  .btn-ghost:hover {
    color: var(--text);
    border-color: var(--border-hover);
    background: var(--bg-surface);
    transform: translateY(-1px);
  }

  /* ── Hero ───────────────────────────────────── */
  .hero {
    text-align: center;
    padding: 80px 24px 60px;
    max-width: 860px;
    margin: 0 auto;
    position: relative;
  }
  .hero-badge {
    font-size: 11px;
    letter-spacing: 0.14em;
    color: var(--accent-light);
    margin-bottom: 20px;
    font-weight: 700;
    animation: fadeIn 0.5s ease-out both;
  }
  .hero h1 {
    font-size: clamp(32px, 5.5vw, 52px);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.04em;
    margin-bottom: 24px;
    animation: fadeInUp 0.5s ease-out 0.1s both;
  }
  .hero-dim { color: var(--text-muted); }
  .hero-sub {
    font-size: 16px;
    line-height: 1.7;
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto 32px;
    animation: fadeInUp 0.5s ease-out 0.2s both;
  }
  .hero-cta {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    margin-bottom: 12px;
    flex-wrap: wrap;
    animation: fadeInUp 0.5s ease-out 0.3s both;
  }
  .hero-note {
    font-size: 11px;
    color: var(--text-muted);
    margin-bottom: 48px;
    animation: fadeIn 0.5s ease-out 0.4s both;
  }

  /* ── Scanner HUD ───────────────────────────── */
  .hud {
    max-width: 600px;
    margin: 0 auto;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    text-align: left;
    background: var(--bg);
    box-shadow: 0 0 80px rgba(249, 115, 22, 0.08), 0 0 160px rgba(249, 115, 22, 0.03), var(--shadow-xl);
    animation: fadeInScale 0.6s ease-out 0.5s both;
  }
  .hud-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 16px;
    background: var(--bg-surface);
    border-bottom: 1px solid var(--border);
  }
  .hud-status {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 10px;
    color: var(--accent-light);
    letter-spacing: 0.08em;
  }
  .hud-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
    animation: pulse 2s ease-in-out infinite;
  }
  @keyframes pulse {
    0%, 100% { opacity: 1; box-shadow: 0 0 4px var(--accent); }
    50% { opacity: 0.5; box-shadow: 0 0 8px var(--accent-glow); }
  }
  .hud-label {
    font-size: 10px;
    color: var(--text-muted);
  }
  .hud-body {
    padding: 16px;
    font-size: 12px;
    line-height: 2;
  }
  .hud-line {
    color: var(--text-muted);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .hud-line.hud-result { color: var(--text-secondary); }
  .hud-ts {
    color: var(--text-muted);
    margin-right: 12px;
    font-size: 10px;
  }
  .hud-accent { color: var(--accent-light); }
  .hud-warn { color: #EAB308; }

  /* ── Scanline animation on HUD ─────────────── */
  .hud::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--accent-glow), transparent);
    animation: scanline 4s ease-in-out infinite;
    pointer-events: none;
  }
  @keyframes scanline {
    0% { top: 0; opacity: 0; }
    10% { opacity: 1; }
    90% { opacity: 1; }
    100% { top: 100%; opacity: 0; }
  }

  /* ── How it works ──────────────────────────── */
  .how {
    max-width: 900px;
    margin: 0 auto;
    padding: 80px 24px;
  }
  .how h2 {
    text-align: center;
    font-size: 28px;
    font-weight: 700;
    margin-bottom: 48px;
    letter-spacing: -0.02em;
  }
  .pipeline {
    display: flex;
    align-items: flex-start;
    justify-content: center;
  }
  .pipe-step {
    flex: 0 1 200px;
    padding: 0 16px;
    text-align: center;
    transition: transform 0.2s ease;
  }
  .pipe-step:hover {
    transform: translateY(-4px);
  }
  .pipe-icon {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    background: var(--accent-dim);
    border: 1px solid rgba(249, 115, 22, 0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 12px;
    transition: all 0.2s ease;
    box-shadow: 0 0 16px rgba(249, 115, 22, 0.05);
  }
  .pipe-step:hover .pipe-icon {
    box-shadow: 0 0 24px var(--accent-glow);
    border-color: rgba(249, 115, 22, 0.3);
  }
  .pipe-num {
    font-size: 12px;
    font-weight: 700;
    color: var(--accent);
    opacity: 0.6;
    margin-bottom: 4px;
  }
  .pipe-label {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 8px;
  }
  .pipe-step p {
    font-size: 13px;
    color: var(--text-secondary);
    line-height: 1.6;
  }
  .pipe-connector {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    padding-top: 24px;
    width: 40px;
  }
  .pipe-line {
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, var(--border), var(--accent-dim), var(--border));
  }

  /* ── Features ──────────────────────────────── */
  .features {
    max-width: 1000px;
    margin: 0 auto;
    padding: 80px 24px;
  }
  .features h2 {
    text-align: center;
    font-size: 28px;
    font-weight: 700;
    margin-bottom: 48px;
    letter-spacing: -0.02em;
  }
  .feat-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
  }
  .feat {
    padding: 28px;
    background: var(--bg);
    transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .feat:hover {
    background: var(--bg-surface);
    transform: translateY(-2px);
  }
  .feat-head {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 12px;
  }
  .feat h3 {
    font-size: 15px;
    font-weight: 600;
  }
  .feat p {
    font-size: 13px;
    color: var(--text-secondary);
    line-height: 1.65;
  }
  .feat-link {
    display: inline-block;
    font-size: 12px;
    color: var(--accent);
    text-decoration: none;
    margin-top: 8px;
    font-weight: 500;
  }
  .feat-link:hover { text-decoration: underline; }

  /* Use cases grid */
  .use-cases-section {
    max-width: 920px;
    margin: 0 auto;
    padding: 80px 24px;
    text-align: center;
  }
  .use-cases-section h2 {
    font-size: 24px;
    margin-bottom: 32px;
  }
  .uc-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    text-align: left;
  }
  .uc-card {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: 16px 20px;
    border: 1px solid var(--border);
    border-radius: 8px;
    text-decoration: none;
    color: var(--text);
    transition: border-color 0.15s;
  }
  .uc-card:hover { border-color: var(--accent); }
  .uc-title { font-size: 13px; font-weight: 600; }
  .uc-desc { font-size: 12px; color: var(--text-muted); line-height: 1.5; }
  .uc-more { margin-top: 20px; }
  .uc-more a { color: var(--accent); font-size: 13px; text-decoration: none; font-weight: 500; }
  .uc-more a:hover { text-decoration: underline; }

  /* ── Pricing ───────────────────────────────── */
  .pricing {
    max-width: 800px;
    margin: 0 auto;
    padding: 80px 24px;
  }
  .pricing h2 {
    text-align: center;
    font-size: 28px;
    font-weight: 700;
    margin-bottom: 6px;
    letter-spacing: -0.02em;
  }
  .pricing-sub {
    text-align: center;
    font-size: 14px;
    color: var(--text-muted);
    margin-bottom: 40px;
  }
  .price-grid-primary {
    display: grid;
    grid-template-columns: 1fr;
    max-width: 440px;
    margin: 0 auto;
    gap: 16px;
    align-items: stretch;
  }

  .try-free-cloud {
    text-align: center;
    font-size: 0.875rem;
    color: var(--text-muted);
    margin-top: 20px;
    margin-bottom: 0;
    line-height: 1.5;
  }

  .try-free-cloud a {
    color: var(--accent-light);
    font-weight: 600;
    text-decoration: none;
  }

  .try-free-cloud a:hover {
    text-decoration: underline;
  }

  .cloud-plans {
    margin-top: 48px;
    padding-top: 32px;
    border-top: 1px solid var(--border);
  }

  .cloud-plans-label {
    text-align: center;
    font-size: 11px;
    letter-spacing: 0.08em;
    color: var(--text-muted);
    margin-bottom: 20px;
    text-transform: uppercase;
  }

  .price-grid-cloud {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    max-width: 640px;
    margin: 0 auto;
    align-items: stretch;
  }

  .price-card-cloud {
    padding: 20px 18px;
    transform: none;
    box-shadow: var(--shadow-sm);
    display: flex;
    flex-direction: column;
  }

  .price-card-cloud:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .price-card-cloud .price-tier {
    font-size: 15px;
    margin-bottom: 2px;
  }

  .price-amount-cloud {
    font-size: 28px;
    font-weight: 800;
    margin-bottom: 8px;
    letter-spacing: -0.02em;
  }

  .price-desc-cloud {
    font-size: 12px;
    color: var(--text-secondary);
    line-height: 1.5;
    margin-bottom: 16px;
    flex: 1;
  }

  .btn-sm-cloud {
    width: 100%;
    padding: 8px 14px;
    font-size: 12px;
    justify-content: center;
  }

  .price-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    align-items: stretch;
  }
  .price-card {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 32px 24px;
    display: flex;
    flex-direction: column;
    box-shadow: var(--shadow-md);
    transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .price-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }
  .price-card.featured {
    border-color: var(--accent);
    box-shadow: 0 0 80px rgba(249, 115, 22, 0.1), 0 0 160px rgba(249, 115, 22, 0.04), var(--shadow-lg);
    position: relative;
  }
  .price-card.featured:hover {
    box-shadow: 0 0 100px rgba(249, 115, 22, 0.14), 0 0 200px rgba(249, 115, 22, 0.06), var(--shadow-xl);
  }
  .price-badge {
    font-size: 10px;
    color: var(--accent);
    letter-spacing: 0.1em;
    margin-bottom: 8px;
    font-weight: 600;
  }
  .price-tier {
    font-size: 18px;
    font-weight: 700;
    margin-bottom: 4px;
  }
  .price-amount {
    font-size: 48px;
    font-weight: 800;
    margin-bottom: 8px;
    letter-spacing: -0.03em;
  }
  .price-per {
    font-size: 14px;
    font-weight: 400;
    color: var(--text-muted);
  }
  .price-desc {
    font-size: 13px;
    color: var(--text-secondary);
    line-height: 1.5;
    margin-bottom: 20px;
  }
  .price-items {
    margin-bottom: 24px;
    flex: 1;
  }
  .pi {
    font-size: 13px;
    color: var(--text-secondary);
    padding: 7px 0;
    display: flex;
    align-items: center;
    gap: 10px;
    font-family: var(--font-mono);
    font-size: 12px;
  }
  .pi-check {
    color: var(--accent-light);
    font-size: 14px;
    flex-shrink: 0;
  }
  .pi-check.dim {
    color: var(--text-muted);
  }
  /* ── Trust ─────────────────────────────────── */
  .trust {
    max-width: 1000px;
    margin: 0 auto;
    padding: 40px 24px 80px;
  }
  .trust-inner {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 48px 40px;
    text-align: center;
    position: relative;
    overflow: hidden;
    box-shadow: var(--shadow-lg);
  }
  .trust-inner::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--accent-dim), var(--accent-glow), var(--accent-dim), transparent);
  }
  .trust-badge {
    font-size: 10px;
    letter-spacing: 0.12em;
    color: var(--accent-light);
    margin-bottom: 16px;
    font-weight: 600;
  }
  .trust h2 {
    font-size: 22px;
    font-weight: 700;
    line-height: 1.3;
    max-width: 500px;
    margin: 0 auto 16px;
  }
  .trust p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 560px;
    margin: 0 auto 24px;
  }
  .trust-links {
    display: flex;
    justify-content: center;
    gap: 12px;
    flex-wrap: wrap;
  }

  /* ── Bottom CTA ────────────────────────────── */
  .bottom-cta { padding: 0 24px 80px; }
  .cta-inner {
    text-align: center;
    max-width: 560px;
    margin: 0 auto;
    padding: 48px 32px;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--bg-surface);
    box-shadow: var(--shadow-lg);
    position: relative;
    overflow: hidden;
  }
  .cta-inner::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--accent-dim), var(--accent-glow), var(--accent-dim), transparent);
  }
  .cta-inner h2 {
    font-size: 22px;
    font-weight: 700;
    margin-bottom: 4px;
  }
  .cta-buttons {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
  }

  /* ── Footer ────────────────────────────────── */
  .lf { text-align: center; padding: 24px; border-top: 1px solid var(--border); }
  .lf-links { display: flex; justify-content: center; gap: 20px; margin-bottom: 8px; }
  .lf-links a { font-size: 12px; color: var(--text-muted); text-decoration: none; font-family: var(--font-mono); }
  .lf-links a:hover { color: var(--text-secondary); }

  /* ── Mobile hamburger toggle ───────────────── */
  .ln-toggle {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 8px;
    min-height: 44px;
    min-width: 44px;
    align-items: center;
    justify-content: center;
  }
  .ln-toggle span {
    display: block;
    width: 20px;
    height: 2px;
    background: var(--text-secondary);
    border-radius: 1px;
    transition: all 0.25s ease;
  }
  .ln-toggle.active span:nth-child(1) { transform: rotate(45deg) translate(5px, 5px); }
  .ln-toggle.active span:nth-child(2) { opacity: 0; }
  .ln-toggle.active span:nth-child(3) { transform: rotate(-45deg) translate(5px, -5px); }

  /* ── Mobile nav panel ──────────────────────── */
  .ln-mobile {
    display: none;
    flex-direction: column;
    background: var(--bg-surface);
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    z-index: 99;
  }
  .ln-mobile.open { display: flex; }
  .ln-mobile-link {
    display: block;
    padding: 14px 24px;
    color: var(--text-secondary);
    font-size: 14px;
    font-weight: 500;
    font-family: var(--font-mono);
    text-decoration: none;
    border-bottom: 1px solid var(--border);
    transition: color 150ms, background 150ms;
  }
  .ln-mobile-link:hover { color: var(--text); background: var(--bg-hover); }
  .ln-mobile-cta { color: var(--accent-light); font-weight: 600; }

  /* ── Responsive ────────────────────────────── */
  @media (max-width: 700px) {
    .ln-link { display: none; }
    .ln-links .btn-sm { display: none; }
    .ln-toggle { display: flex; }
    .hero { padding: 50px 16px 40px; }
    .hero h1 { font-size: clamp(26px, 7vw, 36px); }
    .pipeline { flex-direction: column; align-items: center; gap: 0; }
    .pipe-connector { width: 1px; height: 30px; padding: 0; }
    .pipe-connector .pipe-line { width: 1px; height: 100%; }
    .pipe-step { flex: none; width: 100%; max-width: 320px; padding: 16px; }
    .feat-grid { grid-template-columns: 1fr; }
    .uc-grid { grid-template-columns: 1fr; }
    .price-grid { grid-template-columns: 1fr; }
    .price-grid-cloud { grid-template-columns: 1fr; }
    .hud { margin: 0 auto; }
    .hud-line { font-size: 11px; }
    .trust-inner { padding: 32px 20px; }
    .how, .features, .pricing { padding: 60px 16px; }
  }
  @media (min-width: 701px) and (max-width: 950px) {
    .feat-grid { grid-template-columns: repeat(2, 1fr); }
  }
</style>

<script>
  const lnToggle = document.getElementById('lnToggle');
  const lnMobile = document.getElementById('lnMobile');
  function closeMobileNav() {
    lnMobile.classList.remove('open');
    lnToggle.classList.remove('active');
    lnToggle.setAttribute('aria-expanded', 'false');
  }
  if (lnToggle && lnMobile) {
    lnToggle.addEventListener('click', () => {
      const isOpen = lnMobile.classList.toggle('open');
      lnToggle.classList.toggle('active', isOpen);
      lnToggle.setAttribute('aria-expanded', String(isOpen));
    });
    // Close on any link click inside the mobile nav
    lnMobile.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', closeMobileNav);
    });
    // Close on tap outside
    document.addEventListener('click', (e) => {
      if (lnMobile.classList.contains('open') &&
          !lnMobile.contains(e.target as Node) &&
          !lnToggle.contains(e.target as Node)) {
        closeMobileNav();
      }
    });
  }

  /* Staggered reveal for HUD lines */
  const hudLines = document.querySelectorAll('.hud-line');
  hudLines.forEach((line, i) => {
    const el = line as HTMLElement;
    el.style.opacity = '0';
    el.style.transform = 'translateY(4px)';
    el.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
    setTimeout(() => {
      el.style.opacity = '1';
      el.style.transform = 'translateY(0)';
    }, 600 + i * 200);
  });
</script>
